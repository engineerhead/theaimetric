# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: The AI Metric
email: admin@theaimetric.com
description: >- # this means to ignore newlines until "baseurl:"
  Explore AI infrastructure, LLM gateways, model routing, provider failover, cost optimization, performance, and observability. Get practical guides, technical deep dives, provider comparisons, benchmarks, and insights for building reliable, cost-efficient AI applications across OpenAI, Anthropic, Google, and other LLM providers.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://theaimetric.com" # the base hostname & protocol for your site, e.g. http://example.com


# Build settings
markdown: kramdown
remote_theme: mmistakes/minimal-mistakes@4.28.1
plugins:
  - jekyll-feed
  - jekyll-include-cache
defaults:
  # _posts
  - scope:
      path: ""
      type: posts
    values:
      layout: single
author:
  name: Umair
analytics:
  provider: "google-gtag"
  google:
    tracking_id: "G-E40B00ZJGJ"
    anonymize_ip: true


# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
